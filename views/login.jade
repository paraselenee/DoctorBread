extends layout

block content
    h1 天牛侠认证
    form(method='post', action='/p/users/login')
        div.form-row
            label
                span ID:
                input(type='text',name='id')
        div.form-row
            label
                span password:
                input(type='text',name='password')
        div.form-row
            input(type='submit')
